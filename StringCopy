#include <stdio.h>

void strcopy(char* str1, char* str2) {
    
    while ((*str2++ = *str1++) != '\0') {
        ;
    }
}

int main() {
    
    char* s = "Hello world";
    char* t;
    
    strcopy(s, t);
    
    printf("%s", t);

    return 0;
}
